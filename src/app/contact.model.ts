export interface Contact {
  id: number;            // Identificador único del contacto.
  nombre: string;        // Nombre del contacto.
  apellido: string;      // Apellido del contacto.
  celular: string;       // Número de celular del contacto.
  correo: string;        // Correo electrónico del contacto.
  direccion: string;     // Dirección del contacto.
  telefono: string;      // Número de teléfono del contacto.
}
